# Claude Cowork 五個排程提示詞

本文件提供五份「可直接複製使用」的 Claude Cowork 排程提示詞（Scheduled Task
prompt），對應每天五個分析時段：**05:00 / 10:00 / 15:00 / 20:00 / 24:00**
（Asia/Taipei）。

> **關於「24:00」**：這是節目表式的記法，代表「當天最後一次」。它的實際觸發時刻
> 是**隔天 00:00**（Asia/Taipei），但邏輯上仍然屬於「前一個日曆日」的最後彙整
> ——也就是說，05:00 / 10:00 / 15:00 / 20:00 這四次分析所寫入的
> `reports/YYYY-MM-DD.md`，最終是由 24:00（隔天 00:00 觸發）這次排程負責收尾。
> 因此第五份提示詞的日期計算方式跟其他四份「不一樣」，請見下方排程五內文的
> 特別說明，**不要**單純複製前四份的日期邏輯。

> **關於認證方式（重要，2026-07 更新）**：每份排程提示詞現在一律在雲端工作區
> 用「帶 GitHub Personal Access Token 的 HTTPS URL」clone repository，**不再**
> 依賴本機資料夾／裝置連線。這是因為排程常在無人值守的時段觸發，如果 push
> 依賴當下裝置是否連線（例如透過本機代理取得 git 認證），裝置沒連線時 push
> 就會失敗——commit 看起來成功，實際上完全沒推送到 GitHub。詳見
> [README「設定 Cowork 排程用的 GitHub Personal Access Token」](../README.md#設定-cowork-排程用的-github-personal-access-token)。
> 下方每份提示詞裡的 `<GITHUB_PAT>` 都只是佔位符——**真正的 token 只能貼在你
> 建立 Scheduled Task 時的 prompt 欄位裡，絕對不可以貼進這份文件或其他任何會
> 被 commit 的檔案**。

> **關於新聞分類（2026-07 更新）**：每個時段的「重要更新」都固定依照四大類別
> 整理——1. 國際重大新聞（政治、外交、重大事件）、2. 臺灣/兩岸相關新聞、
> 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）、4. 財經與市場
> （股市、匯率、重大經濟數據，若有明顯波動請標註）。這四個類別的定義同時記錄
> 在 [`config/categories.json`](../config/categories.json)（文件用途，供人工參考），
> 兩邊如需調整請一併同步修改。24:00 排程彙整的最終公開摘要也依同樣四個類別
> 整理「重要事件」一節。

> **關於「不要求五個時段全部齊全才能發布」（重要，2026-07 更新）**：24:00
> 排程只要當天「至少有一個時段」收集到內容，就會產生最終摘要並把狀態標記為
> ready；即使中途有某次排程執行失敗或忘記觸發、缺少一兩個時段，隔天 08:00
> 的正式發布（GitHub Actions）依然會照常送出，**不會**因為湊不滿五次而卡住
> 整天不發布。只有「五個時段全部沒有內容」這種當天完全沒有收集到任何分析的
> 極端情況，才會維持 `collecting` 狀態、暫不發布。缺漏的時段會清楚寫進最終
> 摘要的「涵蓋範圍說明」區塊、以及送進 Telegram 的「今日重點」開頭，讓讀者
> 知道這份摘要是否完整。詳見下方排程五的第五～七步。

## 使用方式

1. 先依照 README 指示建立一組專屬的 GitHub Personal Access Token（僅
   daily-dispatch 這個 repo、Contents: Read and write 權限）。
2. 在 Claude（Cowork）中建立排程任務（Scheduled Task），共建立 5 個，分別對應
   下面 5 個時段。
3. 每個排程的 cron 建議設定為（皆為 **UTC**，Asia/Taipei = UTC+8，換算後如下）：

   | 時段（Asia/Taipei，節目表式記法） | 實際觸發時刻（Asia/Taipei） | cron（UTC） |
   | --- | --- | --- |
   | 05:00 | 當天 05:00 | `0 21 * * *`（前一天 21:00 UTC） |
   | 10:00 | 當天 10:00 | `0 2 * * *` |
   | 15:00 | 當天 15:00 | `0 7 * * *` |
   | 20:00 | 當天 20:00 | `0 12 * * *` |
   | 24:00 | **隔天 00:00** | `0 16 * * *`（前一天 16:00 UTC） |

4. 每個排程的 prompt 內容，複製下方對應區塊後，把 `<GITHUB_PAT>` 換成你剛建立
   的真實 token 再貼上（其餘不需要修改，已包含所有必要脈絡，因為每次排程觸發
   都是全新的 Session，不會記得先前對話）。
5. 請將下方 `g761007`、`daily-dispatch` 換成你實際使用的 GitHub 使用者名稱與
   repository 名稱（若與預設值相同則不需修改）。

## 共用規則（五個排程都適用）

- 搜尋與整理內容時，一律以下列四大類別為主軸（定義見
  [`config/categories.json`](../config/categories.json)），對應【第四步：寫入
  內容】底下的四個分類小節：
  1. 國際重大新聞（政治、外交、重大事件）
  2. 臺灣/兩岸相關新聞
  3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
  4. 財經與市場（股市、匯率、重大經濟數據，若有明顯波動請標註）
  不屬於這四類、但重要性很高的新聞，放進最相近的類別即可，並在內容中說明。
- 一律使用 **Asia/Taipei** 日期與時間，不可直接使用執行環境的 UTC 日期。
- 一律在雲端工作區用帶 token 的 HTTPS URL 全新 clone repository，不使用本機
  資料夾／裝置連線；不執行會把帶 token 的 URL 印出來的指令（例如
  `git remote -v`）。
- 只更新 `reports/YYYY-MM-DD.md`，使用文件中規定的固定 `<!-- slot: HH:MM:start -->`
  / `<!-- slot: HH:MM:end -->` 標記；同一時段重跑時「取代」該時段內容，不可重複
  附加、不可刪除或修改其他時段。
- 不可呼叫 Telegram、不可觸發或執行 `.github/workflows/publish-daily.yml`
  正式發布 workflow、不可讀取或寫入任何 GitHub Secrets。
- 完成後必須將變更 commit 並 push 到預設分支（`main`）。
- 全程使用台灣正體中文撰寫分析內容。

---

## 排程一：05:00 分析

```
你現在要執行 daily-dispatch 專案的每日新聞分析排程（第 1 / 5 個時段：05:00，Asia/Taipei）。

【專案位置與認證】
- GitHub repository：https://github.com/g761007/daily-dispatch
- 一律在雲端工作區操作，不使用本機資料夾／裝置連線（避免依賴當下裝置是否連線
  的不穩定性——本機連線斷開會導致 push 失敗）。
- 用下列指令 clone 一份全新的 repository 到雲端工作區（<GITHUB_PAT> 是專屬這組
  自動化使用的 GitHub Personal Access Token，僅有 daily-dispatch 這個 repo 的
  Contents: Read and write 權限；這個值只存在於這份 Scheduled Task 的設定裡，
  絕對不可以寫進任何會被 commit 的檔案、不可以出現在任何輸出或 log 裡，也不要
  執行 `git remote -v` 之類會把帶 token 的 URL 印出來的指令）：

  git clone https://<GITHUB_PAT>@github.com/g761007/daily-dispatch.git /tmp/daily-dispatch
  cd /tmp/daily-dispatch

- 後面所有讀取、寫入、commit、push 都在這個乾淨的 /tmp/daily-dispatch clone 裡
  進行；push 時直接用 `git push origin main` 即可（clone 時 remote 已經帶好
  認證，不需要再另外設定）。

【第一步：確認日期與時段】
1. 取得目前 Asia/Taipei 的日期（YYYY-MM-DD）與時間，不可使用系統預設 UTC 日期。
   本次觸發時刻就落在當天 05:00 附近，日期直接使用 Asia/Taipei「今天」的日期即可。
2. 本次負責的時段固定為「05:00」。

【第二步：讀取或建立當日檔案】
1. 讀取 reports/YYYY-MM-DD.md（YYYY-MM-DD 為 Asia/Taipei 今天日期）。
2. 若檔案不存在，先建立檔案，內容需包含：
   - 檔案開頭 `# YYYY-MM-DD 每日分析`
   - `<!-- status: collecting -->`
   - 五個時段（05:00 / 10:00 / 15:00 / 20:00 / 24:00）各自的
     `<!-- slot: HH:MM:start -->` 與 `<!-- slot: HH:MM:end -->` 標記（可先留空，
     待各排程依序填入）。

【第三步：搜尋與分析】
1. 搜尋「自上一時段以來」的重要新消息（第一次執行時，搜尋近期重要新聞）。
2. 優先使用可信、可追溯來源；避免只轉貼標題；合併相同事件的重複報導。
3. 明確標註尚未證實的消息，不可把推測寫成已確認事實。
4. 若本時段沒有值得報告的新消息，內容必須寫：
   「本時段沒有發現足以改變今日判斷的重要新進展。」
   不可為了填滿內容而加入低品質新聞。

【第四步：寫入內容】
依照以下格式，寫入（或取代）reports/YYYY-MM-DD.md 中
`<!-- slot: 05:00:start -->` 到 `<!-- slot: 05:00:end -->` 之間的內容：

## 05:00 分析

### 重要更新（依四大類別整理，定義見 config/categories.json）

依序寫出下面四個類別小節，**四個標題都要保留**；某類別本時段沒有值得報告的
新消息時，該小節底下直接寫「本時段本類別無重要更新。」，不可省略標題、也不
可為了填滿內容而加入低品質新聞。

#### 1. 國際重大新聞（政治、外交、重大事件）

##### 事件標題
- 事件摘要：
- 最新進展：
- 重要性：
- 可能影響：
- 後續觀察：
- 來源：
  - [來源名稱](來源網址)

#### 2. 臺灣/兩岸相關新聞
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 4. 財經與市場（股市、匯率、重大經濟數據）
（格式同上；若股市、匯率或重大經濟數據出現明顯波動，請在事件標題前加註
「⚠️ 波動」，並在「重要性」欄位說明波動幅度與可能原因；若無新消息則寫
「本時段本類別無重要更新。」）

### 趨勢觀察
（本時段觀察到的跨類別共同趨勢）

### 低確定性消息
（尚未獲得足夠確認的內容，可跨類別彙整；若無則寫「無」）

規則：
- 若該時段標記已存在，「取代」原有內容，不可重複附加。
- 不可修改或刪除其他時段（10:00 / 15:00 / 20:00 / 24:00）的內容。
- 保持 `<!-- status: collecting -->`（本排程不可將狀態改為 ready）。

【第五步：送出】
1. 確認變更只影響 reports/YYYY-MM-DD.md 的 05:00 區塊。
2. git add、commit（訊息例如：`content: 2026-07-22 05:00 分析`）、push 到 main。
3. 不呼叫 Telegram、不觸發 publish-daily workflow、不接觸任何 Secrets。
```

---

## 排程二：10:00 分析

```
你現在要執行 daily-dispatch 專案的每日新聞分析排程（第 2 / 5 個時段：10:00，Asia/Taipei）。

【專案位置與認證】
- GitHub repository：https://github.com/g761007/daily-dispatch
- 一律在雲端工作區操作，不使用本機資料夾／裝置連線（避免依賴當下裝置是否連線
  的不穩定性——本機連線斷開會導致 push 失敗）。
- 用下列指令 clone 一份全新的 repository 到雲端工作區（<GITHUB_PAT> 是專屬這組
  自動化使用的 GitHub Personal Access Token，僅有 daily-dispatch 這個 repo 的
  Contents: Read and write 權限；這個值只存在於這份 Scheduled Task 的設定裡，
  絕對不可以寫進任何會被 commit 的檔案、不可以出現在任何輸出或 log 裡，也不要
  執行 `git remote -v` 之類會把帶 token 的 URL 印出來的指令）：

  git clone https://<GITHUB_PAT>@github.com/g761007/daily-dispatch.git /tmp/daily-dispatch
  cd /tmp/daily-dispatch

- 後面所有讀取、寫入、commit、push 都在這個乾淨的 /tmp/daily-dispatch clone 裡
  進行；push 時直接用 `git push origin main` 即可（clone 時 remote 已經帶好
  認證，不需要再另外設定）。

【第一步：確認日期與時段】
1. 取得目前 Asia/Taipei 的日期（YYYY-MM-DD）與時間。本次觸發時刻落在當天 10:00
   附近，日期直接使用 Asia/Taipei「今天」的日期即可。
2. 本次負責的時段固定為「10:00」。

【第二步：讀取當日檔案】
1. 讀取 reports/YYYY-MM-DD.md。若不存在，先依照與 05:00 排程相同規則建立骨架
   （含 `<!-- status: collecting -->` 與五個時段標記）。

【第三步：搜尋與分析】
1. 搜尋「自 05:00 排程之後」出現的重要新消息。
2. 對於 05:00 已經報導、10:00 有新進展的事件，應該「更新」該事件，而不是重複
   建立相同項目。
3. 優先使用可信、可追溯來源；避免只轉貼標題；合併相同事件的重複報導。
4. 明確標註尚未證實的消息，不可把推測寫成已確認事實。
5. 若本時段沒有值得報告的新消息，內容必須寫：
   「本時段沒有發現足以改變今日判斷的重要新進展。」

【第四步：寫入內容】
依照以下格式，寫入（或取代）reports/YYYY-MM-DD.md 中
`<!-- slot: 10:00:start -->` 到 `<!-- slot: 10:00:end -->` 之間的內容：

## 10:00 分析

### 重要更新（依四大類別整理，定義見 config/categories.json）

依序寫出下面四個類別小節，**四個標題都要保留**；某類別本時段沒有值得報告的
新消息時，該小節底下直接寫「本時段本類別無重要更新。」，不可省略標題、也不
可為了填滿內容而加入低品質新聞。

#### 1. 國際重大新聞（政治、外交、重大事件）

##### 事件標題
- 事件摘要：
- 最新進展：
- 重要性：
- 可能影響：
- 後續觀察：
- 來源：
  - [來源名稱](來源網址)

#### 2. 臺灣/兩岸相關新聞
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 4. 財經與市場（股市、匯率、重大經濟數據）
（格式同上；若股市、匯率或重大經濟數據出現明顯波動，請在事件標題前加註
「⚠️ 波動」，並在「重要性」欄位說明波動幅度與可能原因；若無新消息則寫
「本時段本類別無重要更新。」）

### 趨勢觀察
（本時段觀察到的跨類別共同趨勢）

### 低確定性消息
（尚未獲得足夠確認的內容，可跨類別彙整；若無則寫「無」）

規則：
- 若該時段標記已存在，「取代」原有內容，不可重複附加。
- 不可修改或刪除其他時段（05:00 / 15:00 / 20:00 / 24:00）的內容。
- 保持 `<!-- status: collecting -->`。

【第五步：送出】
1. 確認變更只影響 reports/YYYY-MM-DD.md 的 10:00 區塊。
2. git add、commit（訊息例如：`content: 2026-07-22 10:00 分析`）、push 到 main。
3. 不呼叫 Telegram、不觸發 publish-daily workflow、不接觸任何 Secrets。
```

---

## 排程三：15:00 分析

```
你現在要執行 daily-dispatch 專案的每日新聞分析排程（第 3 / 5 個時段：15:00，Asia/Taipei）。

【專案位置與認證】
- GitHub repository：https://github.com/g761007/daily-dispatch
- 一律在雲端工作區操作，不使用本機資料夾／裝置連線（避免依賴當下裝置是否連線
  的不穩定性——本機連線斷開會導致 push 失敗）。
- 用下列指令 clone 一份全新的 repository 到雲端工作區（<GITHUB_PAT> 是專屬這組
  自動化使用的 GitHub Personal Access Token，僅有 daily-dispatch 這個 repo 的
  Contents: Read and write 權限；這個值只存在於這份 Scheduled Task 的設定裡，
  絕對不可以寫進任何會被 commit 的檔案、不可以出現在任何輸出或 log 裡，也不要
  執行 `git remote -v` 之類會把帶 token 的 URL 印出來的指令）：

  git clone https://<GITHUB_PAT>@github.com/g761007/daily-dispatch.git /tmp/daily-dispatch
  cd /tmp/daily-dispatch

- 後面所有讀取、寫入、commit、push 都在這個乾淨的 /tmp/daily-dispatch clone 裡
  進行；push 時直接用 `git push origin main` 即可（clone 時 remote 已經帶好
  認證，不需要再另外設定）。

【第一步：確認日期與時段】
1. 取得目前 Asia/Taipei 的日期（YYYY-MM-DD）與時間。本次觸發時刻落在當天 15:00
   附近，日期直接使用 Asia/Taipei「今天」的日期即可。
2. 本次負責的時段固定為「15:00」。

【第二步：讀取當日檔案】
1. 讀取 reports/YYYY-MM-DD.md。若不存在，先依照與 05:00 排程相同規則建立骨架。

【第三步：搜尋與分析】
1. 搜尋「自 10:00 排程之後」出現的重要新消息。
2. 對於先前時段已有進展的事件，應該「更新」而不是重複建立。
3. 優先使用可信、可追溯來源；避免只轉貼標題；合併相同事件的重複報導。
4. 明確標註尚未證實的消息，不可把推測寫成已確認事實。
5. 若本時段沒有值得報告的新消息，內容必須寫：
   「本時段沒有發現足以改變今日判斷的重要新進展。」

【第四步：寫入內容】
依照以下格式，寫入（或取代）reports/YYYY-MM-DD.md 中
`<!-- slot: 15:00:start -->` 到 `<!-- slot: 15:00:end -->` 之間的內容：

## 15:00 分析

### 重要更新（依四大類別整理，定義見 config/categories.json）

依序寫出下面四個類別小節，**四個標題都要保留**；某類別本時段沒有值得報告的
新消息時，該小節底下直接寫「本時段本類別無重要更新。」，不可省略標題、也不
可為了填滿內容而加入低品質新聞。

#### 1. 國際重大新聞（政治、外交、重大事件）

##### 事件標題
- 事件摘要：
- 最新進展：
- 重要性：
- 可能影響：
- 後續觀察：
- 來源：
  - [來源名稱](來源網址)

#### 2. 臺灣/兩岸相關新聞
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 4. 財經與市場（股市、匯率、重大經濟數據）
（格式同上；若股市、匯率或重大經濟數據出現明顯波動，請在事件標題前加註
「⚠️ 波動」，並在「重要性」欄位說明波動幅度與可能原因；若無新消息則寫
「本時段本類別無重要更新。」）

### 趨勢觀察
（本時段觀察到的跨類別共同趨勢）

### 低確定性消息
（尚未獲得足夠確認的內容，可跨類別彙整；若無則寫「無」）

規則：
- 若該時段標記已存在，「取代」原有內容，不可重複附加。
- 不可修改或刪除其他時段（05:00 / 10:00 / 20:00 / 24:00）的內容。
- 保持 `<!-- status: collecting -->`。

【第五步：送出】
1. 確認變更只影響 reports/YYYY-MM-DD.md 的 15:00 區塊。
2. git add、commit（訊息例如：`content: 2026-07-22 15:00 分析`）、push 到 main。
3. 不呼叫 Telegram、不觸發 publish-daily workflow、不接觸任何 Secrets。
```

---

## 排程四：20:00 分析

```
你現在要執行 daily-dispatch 專案的每日新聞分析排程（第 4 / 5 個時段：20:00，Asia/Taipei）。

【專案位置與認證】
- GitHub repository：https://github.com/g761007/daily-dispatch
- 一律在雲端工作區操作，不使用本機資料夾／裝置連線（避免依賴當下裝置是否連線
  的不穩定性——本機連線斷開會導致 push 失敗）。
- 用下列指令 clone 一份全新的 repository 到雲端工作區（<GITHUB_PAT> 是專屬這組
  自動化使用的 GitHub Personal Access Token，僅有 daily-dispatch 這個 repo 的
  Contents: Read and write 權限；這個值只存在於這份 Scheduled Task 的設定裡，
  絕對不可以寫進任何會被 commit 的檔案、不可以出現在任何輸出或 log 裡，也不要
  執行 `git remote -v` 之類會把帶 token 的 URL 印出來的指令）：

  git clone https://<GITHUB_PAT>@github.com/g761007/daily-dispatch.git /tmp/daily-dispatch
  cd /tmp/daily-dispatch

- 後面所有讀取、寫入、commit、push 都在這個乾淨的 /tmp/daily-dispatch clone 裡
  進行；push 時直接用 `git push origin main` 即可（clone 時 remote 已經帶好
  認證，不需要再另外設定）。

【第一步：確認日期與時段】
1. 取得目前 Asia/Taipei 的日期（YYYY-MM-DD）與時間。本次觸發時刻落在當天 20:00
   附近，日期直接使用 Asia/Taipei「今天」的日期即可。
2. 本次負責的時段固定為「20:00」。

【第二步：讀取當日檔案】
1. 讀取 reports/YYYY-MM-DD.md。若不存在，先依照與 05:00 排程相同規則建立骨架。

【第三步：搜尋與分析】
1. 搜尋「自 15:00 排程之後」出現的重要新消息。
2. 對於先前時段已有進展的事件，應該「更新」而不是重複建立。
3. 優先使用可信、可追溯來源；避免只轉貼標題；合併相同事件的重複報導。
4. 明確標註尚未證實的消息，不可把推測寫成已確認事實。
5. 若本時段沒有值得報告的新消息，內容必須寫：
   「本時段沒有發現足以改變今日判斷的重要新進展。」

【第四步：寫入內容】
依照以下格式，寫入（或取代）reports/YYYY-MM-DD.md 中
`<!-- slot: 20:00:start -->` 到 `<!-- slot: 20:00:end -->` 之間的內容：

## 20:00 分析

### 重要更新（依四大類別整理，定義見 config/categories.json）

依序寫出下面四個類別小節，**四個標題都要保留**；某類別本時段沒有值得報告的
新消息時，該小節底下直接寫「本時段本類別無重要更新。」，不可省略標題、也不
可為了填滿內容而加入低品質新聞。

#### 1. 國際重大新聞（政治、外交、重大事件）

##### 事件標題
- 事件摘要：
- 最新進展：
- 重要性：
- 可能影響：
- 後續觀察：
- 來源：
  - [來源名稱](來源網址)

#### 2. 臺灣/兩岸相關新聞
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 4. 財經與市場（股市、匯率、重大經濟數據）
（格式同上；若股市、匯率或重大經濟數據出現明顯波動，請在事件標題前加註
「⚠️ 波動」，並在「重要性」欄位說明波動幅度與可能原因；若無新消息則寫
「本時段本類別無重要更新。」）

### 趨勢觀察
（本時段觀察到的跨類別共同趨勢）

### 低確定性消息
（尚未獲得足夠確認的內容，可跨類別彙整；若無則寫「無」）

規則：
- 若該時段標記已存在，「取代」原有內容，不可重複附加。
- 不可修改或刪除其他時段（05:00 / 10:00 / 15:00 / 24:00）的內容。
- 保持 `<!-- status: collecting -->`。

【第五步：送出】
1. 確認變更只影響 reports/YYYY-MM-DD.md 的 20:00 區塊。
2. git add、commit（訊息例如：`content: 2026-07-22 20:00 分析`）、push 到 main。
3. 不呼叫 Telegram、不觸發 publish-daily workflow、不接觸任何 Secrets。
```

---

## 排程五：24:00 分析＋最終每日摘要（實際觸發時刻為隔天 00:00）

```
你現在要執行 daily-dispatch 專案的每日新聞分析排程（第 5 / 5 個時段：24:00，Asia/Taipei）。
這是「當天最後一個」分析排程，除了完成 24:00 分析，還需要視情況產生當日的最終公開摘要。

【非常重要：日期換算，跟其他四份排程不一樣】
「24:00」是節目表式記法，代表「當天最後一次」，但本排程實際觸發時刻是
**隔天 00:00**（Asia/Taipei）。也就是說：
1. 先取得目前 Asia/Taipei 的「實際日期」（例如觸發當下是 2026-07-23 00:00）。
2. 本排程要處理的目標日期 = 實際日期「減一天」（沿用上例，目標日期就是
   2026-07-22）——因為 05:00 / 10:00 / 15:00 / 20:00 這四次分析都是在
   「2026-07-22」這一天寫入的，24:00 只是它們的收尾。
3. 後面所有步驟中的 YYYY-MM-DD，指的都是這個「減一天」後的目標日期，
   不是觸發當下的實際日期。

【專案位置與認證】
- GitHub repository：https://github.com/g761007/daily-dispatch
- 一律在雲端工作區操作，不使用本機資料夾／裝置連線（避免依賴當下裝置是否連線
  的不穩定性——本機連線斷開會導致 push 失敗）。
- 用下列指令 clone 一份全新的 repository 到雲端工作區（<GITHUB_PAT> 是專屬這組
  自動化使用的 GitHub Personal Access Token，僅有 daily-dispatch 這個 repo 的
  Contents: Read and write 權限；這個值只存在於這份 Scheduled Task 的設定裡，
  絕對不可以寫進任何會被 commit 的檔案、不可以出現在任何輸出或 log 裡，也不要
  執行 `git remote -v` 之類會把帶 token 的 URL 印出來的指令）：

  git clone https://<GITHUB_PAT>@github.com/g761007/daily-dispatch.git /tmp/daily-dispatch
  cd /tmp/daily-dispatch

- 後面所有讀取、寫入、commit、push 都在這個乾淨的 /tmp/daily-dispatch clone 裡
  進行；push 時直接用 `git push origin main` 即可（clone 時 remote 已經帶好
  認證，不需要再另外設定）。

===== 第一部分：完成 24:00 分析 =====

【第一步：確認目標日期與時段】
1. 依照上方「日期換算」步驟，算出目標日期 YYYY-MM-DD。
2. 本次負責的時段固定為「24:00」。

【第二步：讀取當日檔案】
1. 讀取 reports/YYYY-MM-DD.md（YYYY-MM-DD 為換算後的目標日期）。若不存在，先
   依照與其他排程相同規則建立骨架（這種情況代表前面時段可能都漏跑，稍後彙整
   摘要時務必如實反映缺漏）。

【第三步：搜尋與分析】
1. 搜尋「自 20:00 排程之後」出現的重要新消息。
2. 對於先前時段已有進展的事件，應該「更新」而不是重複建立。
3. 優先使用可信、可追溯來源；避免只轉貼標題；合併相同事件的重複報導。
4. 明確標註尚未證實的消息，不可把推測寫成已確認事實。
5. 若本時段沒有值得報告的新消息，內容必須寫：
   「本時段沒有發現足以改變今日判斷的重要新進展。」

【第四步：寫入內容】
依照以下格式，寫入（或取代）reports/YYYY-MM-DD.md 中
`<!-- slot: 24:00:start -->` 到 `<!-- slot: 24:00:end -->` 之間的內容（格式與其他
時段相同）：

## 24:00 分析

### 重要更新（依四大類別整理，定義見 config/categories.json）

依序寫出下面四個類別小節，**四個標題都要保留**；某類別本時段沒有值得報告的
新消息時，該小節底下直接寫「本時段本類別無重要更新。」，不可省略標題、也不
可為了填滿內容而加入低品質新聞。

#### 1. 國際重大新聞（政治、外交、重大事件）

##### 事件標題
- 事件摘要：
- 最新進展：
- 重要性：
- 可能影響：
- 後續觀察：
- 來源：
  - [來源名稱](來源網址)

#### 2. 臺灣/兩岸相關新聞
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
（格式同上；若無新消息則寫「本時段本類別無重要更新。」）

#### 4. 財經與市場（股市、匯率、重大經濟數據）
（格式同上；若股市、匯率或重大經濟數據出現明顯波動，請在事件標題前加註
「⚠️ 波動」，並在「重要性」欄位說明波動幅度與可能原因；若無新消息則寫
「本時段本類別無重要更新。」）

### 趨勢觀察
（本時段觀察到的跨類別共同趨勢）

### 低確定性消息
（尚未獲得足夠確認的內容，可跨類別彙整；若無則寫「無」）

規則：
- 若該時段標記已存在，「取代」原有內容，不可重複附加。
- 不可修改或刪除其他時段（05:00 / 10:00 / 15:00 / 20:00）的內容。
- 此時仍先保持 `<!-- status: collecting -->`，等第二部分確認完整後才可能改成 ready。

===== 第二部分：檢查完整性並產生最終摘要 =====

【第五步：檢查涵蓋範圍（非常重要：不要求五個時段全部齊全才能發布）】
1. 重新讀取完整的 reports/YYYY-MM-DD.md（目標日期）。
2. 檢查 05:00 / 10:00 / 15:00 / 20:00 / 24:00 五個時段，分別確認
   `<!-- slot: HH:MM:start -->` 與 `<!-- slot: HH:MM:end -->` 之間是否存在且
   有實際內容（不是空的），列出「已完成」與「缺少」的時段清單。
3. **核心原則**：只要「至少有一個時段」收集到內容，就要繼續往下第六步產生
   最終摘要並標記為 ready，**不可以**因為湊不到五個時段就整天不發布。某次
   排程執行失敗、忘記觸發、或中途出錯，都只代表「缺少的那個時段沒有內容」，
   不代表今天不能發布——隔天 08:00 的正式發布必須照常進行，讓已經收集到的
   內容送出去，不要因為等待補齊而卡住。
4. 只有在「五個時段全部缺少內容」（等於今天完全沒有收集到任何分析內容，
   通常代表全部排程都沒有觸發或都失敗）這種極端情況下，才真的沒有東西可以
   發布：
   - 不可將狀態改為 ready。
   - 不可建立或覆蓋 site/_summaries/YYYY-MM-DD.md。
   - 在 reports/YYYY-MM-DD.md 檔案最上方（`<!-- status: collecting -->` 下方）
     加上一行 `<!-- missing_slots: 05:00,10:00,15:00,20:00,24:00 -->`。
   - git add、commit（訊息例如：`content: 2026-07-22 24:00 分析（今日無任何時段內容，暫不發布）`）、
     push 到 main，然後結束，不要往下執行第六步之後的內容。
5. 除了上述「全部缺少」的極端情況，其餘所有情況（包含缺 1～4 個時段）都要
   繼續執行第六步。

【第六步：整理最終摘要（涵蓋範圍以實際收集到的時段為準）】
1. 重新閱讀所有「有內容」的時段。**不可直接拼接原文**，必須重新整理、統整、
   合併重複事件；缺少的時段就是缺少，不要為了補足內容而杜撰或臆測。
2. 保留重要數字、日期、人物、公司與產品名稱；清楚區分事實、分析與推測。
3. 產生（或覆蓋）site/_summaries/YYYY-MM-DD.md（YYYY-MM-DD 為目標日期），
   格式如下：

---
title: "YYYY-MM-DD 每日新聞摘要"
date: YYYY-MM-DD 23:59:59 +0800
description: "當日重要新聞、產業趨勢與後續觀察"
layout: summary
published: true
---

## 今日重點
（五到十點，整理今天最重要的事件，可註明所屬類別。若本日有時段缺漏，請在
這個區塊最後另外加一行，例如「⚠️ 本日缺少 15:00 時段分析，本摘要僅涵蓋
05:00 / 10:00 / 20:00 / 24:00 時段收集到的內容。」——這個區塊會被完整送進
Telegram 通知，務必讓讀者在通知裡就能看到涵蓋範圍是否完整，不要只寫在
網頁版才看得到的地方。若五個時段全部完成，則不需要加這行。）

## 重要事件

依照四大類別整理（定義見 config/categories.json），四個標題都要保留；某類別
今天完全沒有值得列入的事件時，該小節底下寫「今日無重要事件。」。

### 1. 國際重大新聞（政治、外交、重大事件）
#### 事件名稱
- 發生了什麼：
- 為什麼重要：
- 可能影響：
- 後續觀察：
- 來源：
  - [來源名稱](來源網址)

### 2. 臺灣/兩岸相關新聞
（格式同上；若無則寫「今日無重要事件。」）

### 3. 科技與 AI 產業動態（新產品、重大技術發布、公司動向）
（格式同上；若無則寫「今日無重要事件。」）

### 4. 財經與市場（股市、匯率、重大經濟數據）
（格式同上；股市、匯率或重大經濟數據若有明顯波動，請在事件名稱前加註
「⚠️ 波動」並在「為什麼重要」中說明波動幅度與可能原因；若無則寫
「今日無重要事件。」）

## 今日趨勢
（整合當天事件後得到的主要趨勢）

## 可能影響
（對產業、市場、產品、開發者或一般使用者的影響）

## 明日觀察
（下一個工作日需要持續關注的事件）

## 涵蓋範圍說明
明確列出本日五個時段各自的完成狀況，例如：
「本日完成時段：05:00 / 10:00 / 20:00 / 24:00。缺少時段：15:00
（該時段排程可能執行失敗或未觸發，本摘要不包含該時段獨有的新消息，
但其他時段若有更新同一事件仍會反映在上面的內容中）。」
若五個時段全部完成，則寫「本日五個時段全部完成，摘要涵蓋完整一天。」

## 資訊聲明
本摘要由自動化分析流程產生，重要資訊請以原始來源為準；若本日有時段缺漏，
請一併參考上方「涵蓋範圍說明」。

注意：Front Matter 的 `date` 欄位請使用「目標日期」當天的 `23:59:59 +0800`
（不是觸發當下的實際日期／時間），這樣才能跟 reports 檔案與網頁網址
（`/daily/YYYY-MM-DD/`）的日期保持一致。

4. 網頁版摘要可以完整一些，但仍須保持容易閱讀；來源連結必須保留。
   （Telegram 版本會由 GitHub Actions 另外用 scripts/extract_summary.py 產生，
   只會擷取「今日重點」「今日趨勢」「明日觀察」三個區塊，這一步「不需要」
   你自己再產生 Telegram 版本，也不需要控制在 3000 字元；但正因為 Telegram
   版本不包含「涵蓋範圍說明」，若有缺漏時段，才更需要依上面的說明把提醒
   寫進「今日重點」。）

【第七步：更新狀態】
1. 確認 site/_summaries/YYYY-MM-DD.md 已成功產生且格式正確後，將
   reports/YYYY-MM-DD.md 的 `<!-- status: collecting -->` 改為
   `<!-- status: ready -->`——**即使還缺少一兩個時段，只要摘要已經依現有
   內容產生完成，也要照樣改成 ready**，不要因為等待補齊全部五個時段而讓
   狀態停留在 collecting（這樣隔天 08:00 的正式發布才不會被卡住）。
2. 若第五步走到「五個時段全部缺少內容」的極端情況（本步驟不會走到，因為
   那個分支在第五步就已經結束了），才會保留 `missing_slots` 標記；一般
   缺 1～4 個時段的情況，不需要在 reports 檔案裡另外寫 `missing_slots`
   標記——涵蓋範圍已經清楚記錄在 site/_summaries/YYYY-MM-DD.md 的
   「涵蓋範圍說明」區塊裡了。

【第八步：送出】
1. git add reports/YYYY-MM-DD.md site/_summaries/YYYY-MM-DD.md
2. commit（訊息例如：`content: 2026-07-22 24:00 分析與最終摘要`，若有缺漏
   時段可以在訊息裡註明，例如
   `content: 2026-07-22 24:00 分析與最終摘要（缺 15:00）`）
3. push 到 main。
4. **不要**自己呼叫 Telegram、**不要**觸發或執行 `.github/workflows/publish-daily.yml`
   正式發布 workflow、**不要**接觸任何 Secrets。正式的 Telegram 發送與 GitHub
   Pages 部署，會由 GitHub Actions 在當天 08:00（Asia/Taipei，與 24:00／隔天
   00:00 的分析間隔約 8 小時）的 `Publish Daily Summary` workflow 自動處理——
   只要 reports/YYYY-MM-DD.md 的 status 是 ready 且
   site/_summaries/YYYY-MM-DD.md 存在，該 workflow 就會正常發布，不會因為
   五個時段沒有全部齊全而卡住或失敗。
```
